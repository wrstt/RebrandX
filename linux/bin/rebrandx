#!/bin/sh
# RebrandX — launch the app window (GTK).
# Must run on the system python: it is the one with GTK + WebKit bindings.
ROOT="$(cd "$(dirname "$(readlink -f "$0")")/../.." && pwd)"
exec /usr/bin/python3 "$ROOT/rebrandx/app_gtk.py" "$@"
